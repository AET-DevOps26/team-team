name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      # ---------------- Checkout ----------------
      - name: Checkout
        uses: actions/checkout@v6

      # ---------------- Java + Gradle cache ----------------
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      # ---------------- Node ----------------
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"

      # ---------------- Python ----------------
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      # ---------------- Cache OWASP DB (restore) ----------------
      - name: Restore OWASP Database Cache
        id: owasp-cache
        uses: actions/cache/restore@v5
        with:
          path: |
            data/owasp-data
          key: owasp-${{ runner.os }}-${{ hashFiles('**/*.gradle*') }}
          restore-keys: |
            owasp-${{ runner.os }}-

      # ---------------- Backend quality gate ----------------
      - name: Backend quality gate (compile + check + test)
        working-directory: server
        run: ./gradlew clean check

      # ---------------- OWASP Dependency Check ----------------
      - name: OWASP Dependency Check
        continue-on-error: true
        id: owasp
        working-directory: server
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        run: ./gradlew dependencyCheckAggregate --info --no-configuration-cache

      # ---------------- Cache OWASP DB (save) ----------------
      - name: Save OWASP Database Cache
        if: always()
        uses: actions/cache/save@v5
        with:
          path: |
            data/owasp-data
          key: owasp-${{ runner.os }}-${{ hashFiles('**/*.gradle*') }}

      # ---------------- OWASP PR Summary + Annotations ----------------
      - name: OWASP Summary
        if: always()
        run: |
          echo "## 🔐 Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "Check artifacts for full report." >> $GITHUB_STEP_SUMMARY

      # ---------------- Upload OWASP Report ----------------
      - name: Upload OWASP Report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: reports
          path: |
            server/build/reports
            server/build/**/security-report

      # ---------------- Python tests ----------------
      - name: Test genai
        working-directory: genai
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pytest

      # ---------------- Frontend: lint, test & build ----------------
      - name: Lint, test & build client
        working-directory: client
        run: |
          npm install
          npm run lint
          npm run test
          npm run build
