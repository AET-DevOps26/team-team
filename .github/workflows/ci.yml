name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Test account-service
        run: ./gradlew :account-service:test

      - name: Test transaction-service
        run: ./gradlew :transaction-service:test

      - name: Test orchestrator-service
        run: ./gradlew :orchestrator-service:test

      - name: Test genai
        working-directory: genai
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pytest

      - name: Test client
        working-directory: client
        run: |
          npm install
          npm run test
          npm run build
