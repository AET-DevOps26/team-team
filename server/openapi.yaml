openapi: 3.0.3
info:
  title: Banking Orchestrator API
  version: 1.0.0
paths:
  /api:
    get:
      summary: API index and available endpoints
      responses:
        '200':
          description: API info response
  /api/health:
    get:
      summary: Health status for orchestrator
      responses:
        '200':
          description: Service health response
  /api/dashboard/{accountId}:
    get:
      summary: Get dashboard payload for account
      parameters:
        - in: path
          name: accountId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Dashboard response
  /api/chat:
    post:
      summary: Chat with banking assistant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
      responses:
        '200':
          description: Chat response
